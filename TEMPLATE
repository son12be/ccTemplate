debug:
NAME cc-dbg
SRCDIRS src
FLAGFILE compile_flags.txt
BUILDDIR build
CC clang -O0 -ggdb3 -fsanitize=undefined -DDEBUG
EXT c

release:
NAME cc
SRCDIRS src
FLAGFILE compile_flags.txt
BUILDDIR build
CC clang -O3
EXT c
